name: Entrega Continua Docker (CI/CD)

on:
  push:
    branches: [ "main" ]

jobs:
  construir-y-publicar:
    runs-on: ubuntu-latest
    
    permissions:
      contents: read
      packages: write

    steps:
    - name: Descargar el codigo del repositorio
      uses: actions/checkout@v4

    - name: Iniciar sesion en GitHub Container Registry (GHCR)
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Construir la imagen de Docker
      run: docker build -t ghcr.io/${{ github.repository }}/mi-api-mlops:latest .

    - name: Subir la imagen al Registry
      run: docker push ghcr.io/${{ github.repository }}/mi-api-mlops:latest
